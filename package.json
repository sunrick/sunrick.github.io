{
  "name": "sunrick.github.io",
  "homepage": "./",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-ga": "^2.3.5",
    "react-router-dom": "^4.2.2",
    "sanitize.css": "^5.0.0",
    "styled-components": "^2.2.1",
    "svg.js": "^2.6.3"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "react-scripts": "1.0.13"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "resume": "cp ../resume-print/rickard_sunden_resume.pdf ./src/img/",
    "deploy": "yarn build && cp CNAME build && git add build && git commit -m 'Update site' && git subtree push --prefix build origin master"
  }
}
